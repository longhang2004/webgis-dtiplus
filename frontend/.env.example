# Backend integration is opt-in.
# If VITE_BACKEND_ENABLED is false or undefined, the frontend uses hardcoded static data.
VITE_BACKEND_ENABLED=false

# Backend API URL, used only when VITE_BACKEND_ENABLED=true.
VITE_API_URL=http://localhost:3001/api
